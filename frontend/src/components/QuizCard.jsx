export default function QuizCard({ quiz, completed, onStart }) {
  return <article className={`quiz-card ${completed ? 'is-completed' : ''}`}><div className="quiz-card-top"><div className="quiz-icon">{quiz.subject?.name?.slice(0, 1) || 'Q'}</div>{completed && <span className="completed-badge">Completed</span>}</div><div className="quiz-card-body"><span className="eyebrow">{quiz.subject?.name || 'General'}</span><h3>{quiz.title}</h3><p>{quiz.description || 'Test your knowledge with a focused practice quiz.'}</p><div className="quiz-meta"><span>◷ {quiz.duration} min</span><span>▦ {quiz.totalQuestions} questions</span></div></div><div className="quiz-card-footer">{completed && <span className="card-score"><strong>{completed.percentage}%</strong> best score</span>}<button className="text-button" onClick={() => onStart(quiz)}>{completed ? 'Retake quiz' : 'Start quiz'} <span>→</span></button></div></article>
}