export default function Navbar({ user, onLogout, onNavigate }) {
  return <header className="navbar"><button className="brand" onClick={() => onNavigate('home')}><span className="brand-mark">?</span><span>Quizly</span></button><nav><button className="nav-link" onClick={() => onNavigate('home')}>Explore</button>{user?.role === 'admin' && <button className="nav-link" onClick={() => onNavigate('admin')}>Admin studio</button>}</nav><div className="nav-actions">{user ? <><button className="profile-chip" onClick={() => onNavigate('profile')}><span>{user.name.slice(0, 1)}</span>{user.name}</button><button className="outline-button" onClick={onLogout}>Log out</button></> : <><button className="nav-link" onClick={() => onNavigate('login')}>Log in</button><button className="primary-button small" onClick={() => onNavigate('register')}>Get started</button></>}</div></header>
}